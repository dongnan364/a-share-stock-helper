services:
  - type: web
    name: a-share-stock-helper
    runtime: node
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "npm start"
    envVars:
      - key: HOST
        value: "0.0.0.0"
      - key: PYTHON
        value: "python"
